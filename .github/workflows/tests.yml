name: Tests

# Only on real pushes. The watcher's own state commits carry [skip ci], so
# the 288 of those a day do not trigger this.
on:
  push:
    paths-ignore:
      - "state/**"
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: python test_matching.py
