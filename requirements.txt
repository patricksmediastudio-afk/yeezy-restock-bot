requests>=2.31.0
python-dotenv>=1.0.0

# Gmail sending (default backend)
google-auth>=2.28.0
google-api-python-client>=2.120.0

# Only needed if EMAIL_BACKEND=sendgrid. Imported lazily, safe to remove.
sendgrid>=6.11.0
